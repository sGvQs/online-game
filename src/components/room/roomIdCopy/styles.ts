import { tv } from "tailwind-variants";

export const roomIdCopy = tv({
	slots: {
		button: "flex items-center gap-2 border border-brand-200 rounded-lg px-4 py-2 hover:bg-brand-50 cursor-pointer transition-colors",
	},
});
