import { tv } from "tailwind-variants";

export const meteorBustersGame = tv({
	slots: {
		container: [
			"relative",
			"min-h-screen",
			"w-full",
			"overflow-hidden",
			"font-dot-gothic-16",
		],
	},
});
